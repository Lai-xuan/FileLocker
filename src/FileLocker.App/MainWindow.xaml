<Window x:Class="FileLocker.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="FileLocker" Height="920" Width="1280" MinHeight="600" MinWidth="720"
        WindowStyle="SingleBorderWindow"
        WindowStartupLocation="Manual"
        ResizeMode="CanResize"
        Background="White">

    <!--
        無邊框視窗：拿掉視覺上的 Windows 原生標題列，改由 WebView2 裡的 HTML 自己畫（macOS 風格的
        左上角三顆按鈕），但刻意保留 WindowStyle="SingleBorderWindow"、不用 "None"——WindowStyle="None"
        會在 Win32 層級直接拿掉 WS_CAPTION／WS_THICKFRAME 這些原生視窗樣式，DWM 的原生最大化/還原
        動畫是綁在「這個 HWND 有沒有原生標題列樣式」上，不是綁在「拖曳/縮放行為有沒有模擬對」——
        之前用 WindowChrome 模擬這些行為時，動畫就是因為樣式被拿掉而消失，這是known issue不是
        WindowChrome 沒設定好。

        改成保留原生樣式（讓 DWM 覺得這是一個正常、可動畫的視窗），改在 MainWindow.xaml.cs 的
        WndProc 自己攔截 WM_NCCALCSIZE，把非客戶區（標題列／邊框）視覺上收縮到 0（有樣式、但畫面
        上完全看不到），再攔截 WM_NCHITTEST 自己判斷滑鼠落在哪個縮放邊界——這是 VS Code／
        Windows Terminal／Chromium 在 Win32 上做無邊框視窗的標準手法。標題列拖曳／雙擊最大化／
        右鍵系統選單維持交給 WebView2 的 IsNonClientRegionSupportEnabled（HTML 端 app-region: drag，
        見 MainWindow.xaml.cs 說明），不需要、也不在 WM_NCHITTEST 裡搶著處理。

        圓角、視窗投影：Windows 11 的原生行為在 WindowStyle="SingleBorderWindow" 底下預期會自動保留；
        MainWindow.xaml.cs 裡用 DwmSetWindowAttribute 手動要回圓角的 TryRestoreRoundedCorners
        繼續保留當保險（原本是為了補償 WindowStyle="None" 拿掉圓角，現在即使非必要也不影響行為，
        呼叫失敗會被吞掉）。

        刻意不設 AllowsTransparency="True"：那會強制 WPF 走軟體算圖路徑，WebView2 的
        繪製效能會明顯變差，而且這個 App 也不需要真的透明的視窗。
    -->

    <Grid>
        <!--
            Margin="6"：對應已知的 WebView2 airspace 問題（微軟官方已確認、追蹤中，
            見 MicrosoftEdge/WebView2Feedback#4538）——WebView2 內部是獨立的原生子視窗，
            不是純 WPF 畫的內容，填滿整個視窗時會把邊緣縮放需要的滑鼠事件整個截走，外層（不管是
            WindowChrome 還是現在 MainWindow.xaml.cs 自己處理的 WM_NCHITTEST）都偵測不到，
            導致完全無法拖曳邊緣調整大小。目前唯一已知的解法是留一圈真正的 WPF 空間（不被
            WebView2 佔用）給縮放偵測用，這條窄邊寬度要跟 WM_NCHITTEST 判斷縮放邊界用的
            ResizeBorderThicknessDip 一致。代價是視窗邊緣會有一條窄窄的實色邊，不是完全無縫——
            這是目前技術限制下的取捨，不是設定錯誤。

            拖放檔案支援：不透過 WPF 層級的原生拖放（試過，WebView2 的原生子視窗會把整個
            拖放操作攔死，訊息完全傳不到外層——這是同一種 airspace 問題）。改用 WebView2
            專門為此設計的官方管道：JS 端正常接住 HTML5 drop 事件（這步在 WebView2 內部
            本來就正常運作），再用 postMessageWithAdditionalObjects 把 File 物件連同真正的
            磁碟路徑一起送出來，C# 端收到的是 CoreWebView2File，讀 .Path 屬性即可，
            見 MainWindow.xaml.cs 的 OnWebMessageReceived 說明。這個做法完全不需要
            AllowExternalDrop="False"，維持預設值即可。
        -->
        <wv2:WebView2 x:Name="MainWebView" Margin="6" />
    </Grid>
</Window>
