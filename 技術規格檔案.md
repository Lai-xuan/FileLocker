# FileLocker 技術規格文件

版本：v1.3 | 最後更新：2026-07-24

本文件反映目前已定案的技術方案與實作進度，不保留已經做完的比較/決策過程，只記錄最終結果與現況。

---

## 0. 技術棧

**語言與架構：C#/.NET 10 後端 + WebView2（HTML/CSS/JS，Vue 3）前端 + C++ Shell Extension。**

Explorer 右鍵選單整合與 `.locked` 副檔名關聯本身是「Windows 平台的複雜度」，跟語言選擇無關——不管用哪個語言都得碰 Registry／COM，這部分省不掉。因此把 Registry／COM 限縮在最小範圍（一個獨立的 C++ Shell Extension DLL），其餘商業邏輯、加密、資料存取全部用生態成熟、除錯工具好的 C# 寫；前端全用 HTML/CSS/JS（Vue 3），不受 XAML 樣板限制。前後端透過 WebView2 的 `postMessage` 溝通。

WebView2 Runtime 在 Windows 11 已內建、Windows 10 也幾乎必然透過 Edge 更新內建；缺少時系統會自動用官方 Bootstrapper 補裝，不需要額外寫相容性判斷程式碼。

若未來要跨到 macOS/Linux，Tauri（Rust）會是更好的長期選擇，但屆時 Explorer 專屬機制本來就要重新設計，屬於獨立的里程碑，不影響現在的架構。

---

## 1. 專案總覽

**目標**：製作一個工具，可加密**檔案與資料夾**、集中管理已加密內容，並在原位置留下與原檔名/資料夾名相同、副檔名與本工具關聯的指標檔（`.locked`）。使用者在資源管理器中選取檔案或資料夾後，透過右鍵選單喚出工具，輸入密碼及提示後完成加密／集中管理流程。

**核心特性（已實作）**：
1. 前後端分離架構：C# 後端（Core Engine + Shell 整合）+ HTML/CSS/JS 前端（透過 WebView2 呈現）
2. 三條解鎖路徑：密碼、Passkey（Windows Hello 快速解鎖）、恢復金鑰（一次性顯示的備援碼），使用者逐項目自行選擇要開哪些
3. GUI：加密（支援一次選多個項目）／解密／已加密清單（含使用紀錄）／設定，共四個頁籤
4. 加密後內容改名為 UUID，移至集中管理區（Vault），Vault 位置可透過設定頁自訂並搬移
5. 原位置留下 `.locked` 指標檔（HMAC-SHA256 簽章防竄改）
6. 雙擊 `.locked` 檔案跳出獨立的原生 WPF 密碼小視窗，若該項目有啟用 Passkey 會自動觸發驗證
7. 支援右鍵**批次選取多個檔案/資料夾**一次加密（C++ COM Shell Extension），同一批在清單頁會摺疊成一組，可一次全部解鎖
8. 密碼錯誤鎖定機制，防暴力破解
9. 支援指向雲端同步資料夾以達成跨裝置加密備份（設計已定案，實際多裝置情境尚未測試）
10. 完整使用紀錄，含來源路徑、解鎖方式、還原位置

**尚未實作**：GUI 視覺美化（目前是功能優先的陽春樣式）、多語言（僅有骨架，只有繁體中文一個選項）、正式安裝程式、單一執行個體機制。

---

## 2. 系統架構

```
┌─────────────────────────────────────────────────────────────────┐
│                         FileLocker.App.exe                        │
│  ┌─────────────────────┐        ┌──────────────────────────┐    │
│  │ 前端 (WebView2/Vue 3) │ ↔JS-Bridge↔ │      後端 (C#)             │   │
│  │  - 加密（含批次）      │        │  - LockService（總入口）    │   │
│  │  - 解密               │        │  - Crypto（加解密/金鑰）    │   │
│  │  - 已加密清單/使用紀錄  │        │  - Vault（Vault 存取）      │   │
│  │  - 設定               │        │  - Security（鎖定機制）     │   │
│  └─────────────────────┘        │  - Settings（App 設定）     │   │
│                                    └──────────────────────────┘    │
│  另一個進入點：PasswordPromptWindow（原生 WPF，雙擊 .locked 時用）      │
└─────────────────────────────────────────────────────────────────┘
                      │                                    │
        ┌─────────────┴─────────────┐        ┌─────────────┴─────────────┐
        ▼                           ▼        ▼                           
┌───────────────────┐   ┌─────────────────┐   ┌───────────────────────────┐
│  Vault（可搬移）     │   │  App 本機資料     │   │  Shell Extension（C++ DLL） │
│  {uuid}.enc          │   │ （固定位置，不搬）  │   │  COM IContextMenu，        │
│  {uuid}.meta.json    │   │  settings.json    │   │  處理右鍵選單與多選批次      │
│  vault.config.json   │   │  history.jsonl    │   └───────────────────────────┘
└───────────────────┘   │  lockout.json     │
                          └─────────────────┘
```

`FileLocker.Core` 是獨立的 .NET Class Library，`FileLocker.App`（WPF+WebView2 主程式）、`FileLocker.Cli`（CLI 原型）、測試專案都呼叫同一顆核心。`FileLocker.ShellExtension` 是獨立的原生 C++ 專案，跟 Core Engine 之間沒有直接依賴，只透過啟動 `FileLocker.App.exe` 並傳遞命令列參數溝通。

**單一執行個體機制（已完成）**：`App.xaml.cs` 啟動時用一個具名 Mutex 判斷「這台裝置這個使用者底下，有沒有其他 FileLocker 實體已經在跑」。有的話，這次啟動透過 Named Pipe 把命令列參數轉送給既有的實體、自己不開任何視窗直接結束；沒有的話正常啟動，並額外起一個背景執行緒監聽這個 Pipe，準備接收之後可能發生的轉送請求。收到轉送過來的參數後，跟這次啟動本身的參數走同一套 `HandleLaunchArgs` 邏輯：雙擊 `.locked` 檔案一律開新的 `PasswordPromptWindow`（允許同時解鎖多個不同項目）；加密用的路徑則是有既有的 `MainWindow` 就送進那一個（同時搶回前景焦點），沒有才新開一個。`ShutdownMode` 改成 `OnExplicitShutdown` 手動控制，任一視窗關閉時檢查是否所有視窗都已關閉，是的話才真正結束整個行程。

---

## 3. 加密流程設計

### 3.1 密文串流格式

密文**不是**單一區塊，而是切成一塊一塊（chunk，預設 1MB）分別加密，原因是要支援大檔案串流處理、避免整個檔案內容同時存在記憶體裡。每塊各自獨立驗證完整性：

```
密文串流（重複到結束）：
┌────────────────────────────────────────┐
│ 區塊明文長度 (4 bytes, big-endian)          │
│ Nonce (12 bytes)                          │
│ 密文（長度＝上面那個區塊明文長度）              │
│ Auth Tag (16 bytes)                       │
└────────────────────────────────────────┘
```

Salt、Argon2 參數、驗證雜湊都**不在**這個密文檔案裡，存在對應的 `{uuid}.meta.json`（見第 4 節）。實作在 `FileLocker.Core.Crypto.ChunkedCipher`，底層每一塊用 `AesGcmCipher`（AES-256-GCM）加解密。

### 3.2 資料夾加密設計

**採用「封裝後加密」策略**：資料夾加密時，先用 `System.IO.Compression.ZipArchive` 將整個資料夾打包成一個暫存 zip，再把這個 zip 當成一份「檔案」丟進既有的檔案加密流程，完全複用同一套邏輯。

理由與取捨：
- ✅ 複雜度最低，不需要另外維護一套「多檔 UUID 對應資料夾樹狀結構」的索引邏輯
- ✅ 完整性天然保證：整包資料夾是一個 AEAD 單元，要嘛整包驗證成功、要嘛失敗
- ⚠️ 取捨：解密資料夾時需要暫存空間先解壓縮，非常大的資料夾會需要對應的磁碟暫存空間與時間

**暫存空間**：壓縮成 zip 這一步的暫存檔放在 `%TEMP%\FileLocker\`。加密完成寫入 Vault 後，先用 `SecureFileEraser` 覆寫隨機資料再刪除暫存 zip 與原始資料夾，不是單純 `Delete`。

**巢狀 `.locked` 項目的風險處理**：資料夾加密時若遞迴掃描發現內含既有的 `.locked` 指標檔（代表裡面本身就包著之前單獨加密過的內容），會把內層項目的 UUID 記在外層項目的 `ContainsNestedLocks` 欄位裡。之後使用者在清單頁想**永久刪除**這筆外層紀錄時，如果 `ContainsNestedLocks` 不是空的，會直接被擋下來（白話文案說明原因），只給「先去解鎖」跟「取消」兩個選項，**不提供強制刪除的按鈕**——把「使用者不知情弄丟資料」這個風險直接在操作層面擋死。

### 3.3 加密流程

實作在 `LockService.EncryptAsync`，對應到 GUI 的「加密」頁籤（支援一次選多個項目，見 3.5 節）：

1. 檢查目標位置是否已經有指標檔（先做這個便宜的檢查，才去做壓縮資料夾這種可能很花時間的工作）
2. 純檔案 I/O／加密運算（壓縮、Argon2 衍生金鑰、`ChunkedCipher` 串流加密寫入 Vault）丟進背景執行緒處理，避免卡住 UI
3. 若使用者有勾選「開啟以 Passkey 快速解鎖」，這裡才觸發（見 8.1 節），且刻意留在呼叫端原本的執行緒上直接 `await`（不能丟進背景執行緒，會影響 Windows Hello 驗證視窗的焦點行為）
4. 若使用者有勾選「開啟恢復金鑰備援」，這裡產生並包裝（見 8.2 節）
5. 寫入 `{uuid}.meta.json`、寫入簽章過的 `.locked` 指標檔
6. 安全清除原始明文檔案/資料夾——這一步失敗**不會**讓整個加密結果判定成失敗（此時加密內容已經安全寫入 Vault），只會回傳警告訊息提醒使用者手動處理
7. 記錄到使用紀錄（來源路徑、Passkey／恢復金鑰是否啟用）

### 3.4 解密流程

三條路徑共用同一個核心還原邏輯（`LockService.RestoreFromKey`），差別只在「怎麼拿到內容金鑰」：

- **密碼路徑**（`DecryptAsync`／`DecryptByUuidAsync`）：檢查密碼鎖定狀態（見 8.4 節）→ Argon2 重新衍生 → 比對驗證雜湊 → 通過才拿到金鑰
- **Passkey 路徑**（`DecryptByPasskeyAsync`）：觸發 Windows Hello 簽章 → 衍生包裝金鑰 → 解開內容金鑰
- **恢復金鑰路徑**（`DecryptByRecoveryKeyAsync`）：解析使用者輸入的恢復碼 → 衍生包裝金鑰 → 解開內容金鑰

拿到內容金鑰之後，共用邏輯：

1. 檢查還原目的地是否已有同名檔案/資料夾（有的話直接失敗，不覆蓋）
2. `ChunkedCipher` 串流解密寫到目的地（資料夾的話先寫進暫存 zip 再解壓縮還原）；解密中途失敗（內容損毀/被竄改）會刪除已經寫到一半的不完整檔案，不留下誤導使用者的半成品
3. 刪除 Vault 內對應的 `.enc`／`.meta.json`
4. 反推原本 `.locked` 應該在的位置，**驗證簽章通過**才刪除（不是只比對 UUID，避免被偽造的假指標檔誘導誤刪，見 8.3 節）
5. 記錄使用紀錄（用哪種方式解的鎖、還原到哪裡）

「永久刪除」（清單頁的刪除紀錄操作）不經過解密，直接刪除 Vault 內容，等於資料徹底銷毀且無法復原；同樣會清掉原位置失效的 `.locked` 指標檔（沿用同一套簽章驗證過的清除邏輯）。GUI 文案明確告知這是不可逆的銷毀動作，不是「從清單移除」。

### 3.5 批次加密與批次解鎖

**加密端**：GUI 的「加密」頁籤跟 Shell Extension 右鍵選單都支援一次選多個項目。多個項目時：
- **Passkey／恢復金鑰選項會被鎖住不能勾**——批次開 Passkey 代表每個項目都要重新跳一次 Windows Hello 驗證，體驗太差；批次開恢復金鑰會讓每個項目各自產生不同的碼，顯示/保存的介面複雜度太高。批次加密目前只支援密碼保護。
- 逐一呼叫 `LockService.EncryptAsync`，每完成一個項目就即時回報結果（`encryptItemResult` 訊息），前端即時更新進度，不用等全部跑完才看到結果
- 同一批全部標上同一個隨機產生的 `BatchId`（存在 `LockedItemMetadata.BatchId`），單一項目加密時這個欄位是 `null`

**清單頁摺疊顯示**：分組邏輯完全在前端（Vue）做，後端只負責在每個項目上帶 `batchId`。有 `batchId` 的項目會摺疊成一列，預覽文字取前兩個檔名接「...等N個文件」；展開後每個項目維持完全獨立的操作能力（各自的解密／Passkey／恢復金鑰／永久刪除按鈕）。

**批次解鎖**：摺疊列上的「全部解鎖」只支援密碼（跟批次加密的範圍限制一致），輸入一次密碼、逐一解密清單裡的每個 UUID，每完成一個即時從清單移除，全部跑完後如果有失敗的會提示使用者可以展開個別重試。還原位置固定用各自的原始位置，不像單獨解鎖那樣可以另外指定，避免批次情境下每個項目都要問一次目的地。

---

## 4. Metadata 儲存設計

**每個加密項目一份獨立的 `{uuid}.meta.json`**，與對應的 `{uuid}.enc` 放在一起。選這個設計而不是單一中央資料庫，是因為多裝置透過雲端同步資料夾共用 Vault 時，各自新增/修改的是**不同的獨立小檔案**，天然不會互相衝突。

目前欄位（`LockedItemMetadata`）：原始名稱、原始路徑、密碼驗證雜湊、salt、Argon2 參數、提示文字、型別（檔案/資料夾）、建立時間、巢狀鎖定清單（`ContainsNestedLocks`）、Passkey 相關欄位（`PasskeyEnabled`／`PasskeyCredentialName`／`PasskeyChallenge`／`PasskeyWrappedContentKey`）、恢復金鑰相關欄位（`RecoveryKeyEnabled`／`RecoveryKeyWrappedContentKey`）、批次 ID（`BatchId`）。

**讀取方式**：App 啟動或清單頁按「重新整理」時，直接掃描 Vault 資料夾內所有 `.meta.json`（`VaultManager.ScanAll()`）。原本規劃要另外建一層 SQLite 快取索引 + `FileSystemWatcher` 即時更新，但實際做出來發現手動「重新整理」已經夠用、複雜度也低很多，**目前沒有實作快取層**，這塊先擱置，未來項目數量真的大到需要優化時再考慮加回來。`ScanAll()` 用檔案「內容」裡的 UUID（不是檔名）判斷重複，同一個 UUID 只會回傳一筆——這是雲端同步情境測試時發現並修掉的問題：同步用戶端偵測到寫入衝突時常見的做法是另存一份帶裝置名稱的「衝突副本」檔案，內容相同但檔名不同，不特別處理的話會讓清單頁出現重複列。

`.meta.json` 是明文檔案，沒有像 `.locked` 指標檔那樣的簽章保護，理論上可能被竄改或損毀——已知的相關風險與緩解見 8.3 節「安全性審查紀錄」。

---

## 5. 資源管理器整合機制

### 5.1 為什麼批次（多選）加密需要正規 Shell Extension

Windows 的輕量「Registry 動詞註冊」在使用者**多選檔案**按右鍵時，命令列的 `%1` 只會帶入其中一個檔案路徑，無法拿到完整選取清單。要支援「一次選多個檔案/資料夾加密」，必須實作標準的 **COM `IContextMenu` Shell Extension**，透過 `IDataObject` 取得完整的多選清單。

**另外查證確認的第二個理由**：Windows Explorer 有一個內建限制（微軟官方文件 KB 2022295）——透過簡易登錄檔動詞註冊的右鍵選單項目，使用者選取超過 **15 個**檔案時會直接從選單裡消失（`HKCU\Software\Microsoft\Windows\CurrentVersion\Explorer` 底下的 `MultipleInvokePromptMinimum`，預設值就是 15）。已有其他開發者的 Shell Extension 專案驗證過：這個限制只發生在簡易登錄檔動詞註冊，改用正規 COM `IContextMenu` Shell Extension 完全不受影響。兩個各自獨立的原因，都指向同一個結論：批次加密只能走正規 Shell Extension。

### 5.2 Shell Extension 實作現況（已完成並實測）

`FileLocker.ShellExtension`（獨立的 C++ 專案，`src/FileLocker.ShellExtension/`）實作：

- **`IShellExtInit::Initialize`**：接收 Explorer 傳來的 `IDataObject`，用 `DragQueryFileW` 解析出完整的多選路徑清單
- **`IContextMenu::QueryContextMenu`**：在右鍵選單插入「使用 FileLocker 加密」
- **`IContextMenu::InvokeCommand`**：組出命令列、啟動 `FileLocker.App.exe`

這個 DLL **直接載入到 `explorer.exe` 行程裡面**執行，這也是為什麼堅持用原生 C++（不用 SharpShell 這類把 Shell Extension 包成 .NET 的社群方案）——避免把 .NET Runtime 載入每一個 `explorer.exe`，出錯風險跟系統穩定性直接掛鉤。DLL 本身只做「顯示選單、把選取清單交給主程式」這兩件事，加密邏輯、GUI 全部都在 `FileLocker.App.exe`（C#）裡處理。

**交接方式**（已實作）：預估命令列長度超過 **8,000 字元**、或選取項目數量超過 **50 個**，改成把完整路徑清單寫進一個暫存 `.txt` 檔（UTF-16 LE with BOM），只把這個檔案路徑（前面加 `@` 當標記）當作單一命令列參數傳給主程式；門檻以下直接把每個路徑當獨立命令列參數。`FileLocker.App`（`App.xaml.cs`）收到 `@路徑` 開頭的參數時會反過來讀取這個清單檔。

**已知限制**：
- **`FileLocker.App.exe` 的路徑目前寫死指向開發環境的 build 輸出路徑作為後備**——正式安裝後，Shell Extension DLL 會跟主程式放在同一個資料夾（見下方「自動註冊」說明），優先檢查同資料夾的邏輯已經足以應付正式環境，寫死路徑只在開發階段、DLL 還沒跟主程式放一起時當退路用。
- 編譯時需要在指令加 `/utf-8` 參數（原始碼含中文註解，沒加這個參數 `cl.exe` 會用系統字碼頁誤判編碼導致編譯錯誤），已在文件跟建置流程中確認。

**自動註冊（已完成）**：Shell Extension 的登錄不需要安裝程式知道任何 COM／`regsvr32` 相關的事——`FileLocker.App` 自己在啟動時檢查、需要的話就自動註冊（`FileLocker.App.ShellExtensionRegistrar`）。關鍵設計決定：登錄檔全部寫在 `HKEY_CURRENT_USER\Software\Classes` 底下，不是 `HKEY_CLASSES_ROOT`——這是 Windows 官方支援的每個使用者各自登錄的機制，Explorer 會自動把它併進當前使用者看到的合併視圖裡，效果完全一樣，但**不需要系統管理員權限**，安裝程式本身也不需要為了這件事另外要求提高權限。安裝程式只要負責把 `FileLockerShellExtension.dll` 跟 `FileLocker.App.exe` 放進同一個「應用程式內容資料夾」就好（一般的檔案安裝功能，不需要額外邏輯）。首次偵測到需要註冊（全新安裝，或應用程式資料夾被搬移過）時，會跳出提示告知使用者需要重啟 Windows 檔案總管，右鍵選單才會出現新登錄的項目（Explorer 對 Shell Extension 有自己的快取，不會即時反映登錄檔變化）。開發階段的 `regsvr32` 手動測試也因為這個改動不再需要系統管理員權限。`FileLocker.App.csproj` 的建置流程也加了一個步驟：只要 `FileLocker.ShellExtension` 資料夾底下有編好的 DLL，每次建置都會自動複製到 `FileLocker.App` 的輸出目錄，開發測試不用再手動複製。

### 5.3 `.locked` 副檔名關聯

```
HKEY_CLASSES_ROOT\.locked = "FileLocker.LockedFile"
HKEY_CLASSES_ROOT\FileLocker.LockedFile\shell\open\command
    (預設) = "\"C:\Program Files\FileLocker\FileLocker.App.exe\" \"%1\""
```

雙擊 `.locked` 檔案時，`FileLocker.App.exe` 收到的命令列參數是這個檔案的路徑，`App.xaml.cs` 判斷副檔名是 `.locked` 就直接開 `PasswordPromptWindow`（原生 WPF 小視窗，不載入 WebView2，追求反應速度），不開主視窗。這個副檔名關聯的正式登錄腳本還沒寫，目前開發階段用手動命令列參數模擬雙擊行為測試。

---

## 6. Vault（集中管理區）與 App 設定

**App 本機設定**（`FileLocker.Core.Settings.AppSettings` / `AppSettingsManager`）存在固定位置 `%LocalAppData%\FileLocker\settings.json`，欄位：`VaultPath`、`Language`（目前固定 `zh-TW`）、`Theme`（`light`／`dark`，目前只存偏好，畫面還沒真的套用）。

**Vault 位置**：首次啟動時，如果 `settings.json` 裡的 `VaultPath` 是空的，預設用 `%LocalAppData%\FileLocker\Vault` 並存回設定檔——**目前沒有做成強制使用者確認的精靈流程**，直接套用預設值，之後隨時可以在 GUI 的「設定」頁籤更改。

**搬移 Vault**：設定頁選新資料夾後，`MainWindow.HandleChangeVaultPathRequestAsync` 會：
1. 檢查新位置是否為空資料夾（不是空的直接拒絕，避免跟既有檔案混在一起）
2. 優先用 `Directory.Move`（同一磁碟區內幾乎瞬間完成），失敗（通常是跨磁碟區）就退回逐一複製再刪除來源
3. 更新 `settings.json`
4. **要求使用者手動重新啟動 App 讓變更生效**——刻意不嘗試在執行中的 App 裡熱替換正在使用的 `VaultManager`，避免跟進行中的操作互相干擾，這是簡化過的設計取捨

`vault.config.json` 存放的是什麼金鑰：這個檔案只存一把隨機產生的 **「Vault 簽章金鑰」**（256-bit），用途僅限於 HMAC-SHA256 簽署 `.locked` 指標檔、驗證指標檔沒有被竄改（見第 8 節）。它**不是**、也**不能**拿來解密任何 `.enc` 檔案——真正的加密金鑰是使用者輸入密碼當下即時衍生出來的，不會被儲存在任何地方，使用者忘記密碼一樣無法復原。這把簽章金鑰外洩最壞情況只是有心人能偽造出一個指向錯誤 UUID 的假指標檔（仍然需要正確密碼/Passkey/恢復金鑰才能解密內容），所以可以安全地跟著 Vault 一起用明文存放並隨雲端同步分享給所有裝置。Vault 初次建立時若不存在就產生新的一把，已存在（例如接上既有的同步 Vault）就直接讀取沿用。

**使用紀錄／密碼鎖定狀態不隨 Vault 搬移**：`history.jsonl`、`lockout.json` 都存在固定的 `%LocalAppData%\FileLocker\`，不是 Vault 內容的一部分——這些是「這台裝置」的本機操作留痕/安全狀態，不應該隨 Vault 被雲端同步分享出去。

---

## 7. 雲端同步支援的運作模式

**核心概念：本工具本身不做雲端上傳，而是「把 Vault 資料夾指向使用者既有雲端同步軟體（OneDrive／Dropbox／Google Drive 等）的本機同步資料夾」，讓同步軟體去做它原本就會做的事。**

運作方式：

1. 使用者在設定頁把 Vault 路徑指定為例如 `C:\Users\X\OneDrive\FileLockerVault\`
2. 加密流程完全不變：FileLocker 把 `{uuid}.enc` 與 `{uuid}.meta.json` 寫進這個資料夾
3. OneDrive 用戶端偵測到資料夾內容變化，自動把這些檔案上傳到雲端——**它上傳的是已經加密過的密文**，OneDrive／Dropbox 本身完全看不到明文內容，這是「零知識」的效果
4. 使用者在另一台裝置安裝 FileLocker，登入同一個雲端帳號，同步用戶端會把整個 Vault 資料夾同步下來
5. FileLocker 在新裝置啟動時掃描 Vault，讀到 `.meta.json` 就能重建出「已加密清單」，使用者輸入密碼即可在任一台裝置上解密——**前提是使用者自己要記得密碼，密碼本身不會、也不應該透過雲端同步**

需要提醒使用者的限制：

- `.locked` 指標檔通常留在文件、桌面等一般資料夾，**不在** Vault 內，所以指標檔本身**不會**自動跨裝置同步。在 A 裝置加密的檔案，B 裝置的 Vault 會有對應的 `.enc`，但 B 裝置若沒有那個 `.locked` 指標檔，就要透過「已加密清單」頁面直接解密，而不是雙擊指標檔案。
- **Passkey 不會跨裝置同步**（見 8.1 節），恢復金鑰不受這個限制（本質是一組獨立於裝置的碼）。
- 若兩台裝置幾乎同時對同一個 UUID 做操作，獨立 `.meta.json` 設計能避免資料庫層級的損毀，但檔案本身仍可能出現同步軟體自己的「衝突副本」，這是雲端同步軟體的通用限制，非本工具能完全避免。

**現況**：FileLocker 自己能控制的部分（Vault 可攜性、衝突副本不重複顯示、多實體併發操作不損毀資料）已經用自動化測試驗證過，見第 12 節階段 8。真的跨裝置、真的雲端帳號的完整流程尚待使用者自行實測。

---

## 8. 安全性考量清單

- **原始明文安全清除**：`SecureFileEraser` 對原始檔案／解壓縮後的暫存資料夾做覆寫清除；SSD 上完全清除有其物理限制，需在文件與 UI 中告知使用者。
- **記憶體中金鑰清空**：使用 `CryptographicOperations.ZeroMemory` 主動清除，涵蓋密碼衍生金鑰、Passkey/恢復金鑰包裝金鑰、簽章結果等。
- **AES-GCM 例外處理接基底類別**：`catch (CryptographicException)` 而不是寫死接某個具體子類別（例如 .NET 10 的 `AuthenticationTagMismatchException`），避免未來 .NET 版本更新後接不到、程式直接崩潰。
- **加密流程的「收尾清除」失敗不等於加密失敗**：這一步失敗只回傳警告訊息，不會被誤判成整個加密流程失敗。
- **Vault 內容刪除順序**：`VaultManager.DeleteItem` 先刪 `.meta.json`、後刪 `.enc`，中途中斷的最壞情況是留下孤兒 `.enc`（浪費空間但不會誤導），不是留下指向不存在內容的「幽靈」metadata。
- **Vault 相關檔案一律走原子寫入**：`vault.config.json`、`.meta.json`、`.locked` 指標檔都用「先寫暫存檔、成功後才原子改名」的方式寫入（`FileLocker.Core.Io.AtomicFile`）。
- **`.locked` 指標檔完整性（HMAC-SHA256）**：金鑰是 Vault 初次建立時產生、存在 `vault.config.json` 的「Vault 簽章金鑰」（見第 6 節）。指標檔內容 = `UUID + HMAC-SHA256(UUID, Vault金鑰)`，開啟時重新計算比對，不相符就代表被竄改或指向錯誤 UUID。**解密成功後清除失效指標檔、以及清單頁「永久刪除」時，都要求簽章驗證通過才會真的執行刪除**，不能只比對 UUID（見 8.3 節）。
- **密碼遺失情境**：務必明確告知「密碼遺失=無法復原」，不做後門機制。
- **`.meta.json` 裡的原始檔名／路徑刻意不加密（設計取捨，非漏洞）**：`OriginalName`、`OriginalPath` 存的是明文，任何拿得到 `.meta.json` 的人（包括雲端服務商）看得到「使用者曾經有一個叫什麼名字的檔案放在哪裡」，但看不到、也推不出檔案內容本身，`PasswordVerificationHash`／`PasskeyWrappedContentKey`／`RecoveryKeyWrappedContentKey`／`Salt` 都是不可逆的密文/雜湊。技術上可以把檔名也一起加密、用跟內容一樣的金鑰解回來，但這樣一來「已加密清單」頁就沒辦法在使用者輸入密碼之前顯示每個項目的原始檔名幫助辨認——清單好用跟檔名本身保密是互斥的取捨，目前選擇前者。如果檔名本身就是敏感資訊，這是目前設計沒有涵蓋到的保護範圍，需要在文件/UI 上讓使用者知道。

### 8.1 Passkey 快速解鎖（已完成，功能可用；已知一個未解決的視窗焦點小瑕疵）

**目標**：密碼之外，額外提供「用 Windows Hello（臉部/指紋/PIN）快速解鎖」的選項，逐項目自行選擇要不要開。

**核心原則：Passkey 是密碼之外「額外」的一道門，不是取代密碼**。安全模型從「只有記得密碼的人才能解密」變成「知道密碼、或能通過這台裝置 Windows Hello 驗證的人都能解密」，預設不開啟。

**技術路線**：
- **不能用** `UserConsentVerifier` + 本機儲存的密鑰（例如 DPAPI）——已有實際資安分析案例證實這種做法形同虛設，驗證訊號跟底層資料解不解得開沒有密碼學上的關聯，可以完全繞過。
- **正確做法**：用 `KeyCredentialManager`／`KeyCredential`（`FileLocker.Core.Crypto.PasskeyProtector`）。私鑰永遠留在 TPM 裡不會離開，每次 `RequestSignAsync` 都會重新觸發一次 Windows Hello 驗證。把簽章結果本身拿去做 HKDF 衍生出「包裝金鑰」，讓「有沒有通過 TPM 驗證」直接反映在「衍生不衍生得出正確金鑰」上。
- **簽章決定性已實測確認**：同一把裝置金鑰、同一份挑戰資料，多次簽章結果完全一致，不受 PIN 或生物辨識影響——這是設計成立的必要前提。

**已知限制**：
- **裝置綁定，不會跨裝置同步**——就算兩台裝置登入同一個 Microsoft 帳戶也不會自動同步（Microsoft 帳戶同步的是「登入網站用」的 passkey，跟 `KeyCredentialManager` 是不同機制）。
- **未封裝應用程式的隔離邊界較弱**——沒有 MSIX AppContainer 那種硬隔離，緩解方式是憑證名稱帶專屬 GUID。
- **驗證視窗可能跳到背景，未完全解決**：已實作緩解機制（`FileLocker.Core.Crypto.WindowFocusHelper`：搶前景焦點、開放新視窗搶焦點、主動輪詢釘住新跳出的視窗），對**第一次**（建立金鑰）有效，但正式流程連續觸發兩次驗證時，**第二次的驗證視窗持續沒有自動跳到最前面**，排查過多個方向（真人操作介入、執行緒環境、WinRT 呼叫位置）都沒有找到根本原因，懷疑跟 WebView2 這層瀏覽器引擎的存在有關但未證實，先擱置。**純粹是體驗上的小瑕疵，不影響加密/解密的正確性或安全性**，功能完整可用。兩個獨立測試專案（`PasskeyDeterminismTest`、`PasskeyWpfTest`）予以保留供之後繼續排查。

### 8.2 恢復金鑰（已完成，功能可用）

**目標**：密碼、Passkey 之外的第三條解鎖路徑，給密碼忘記、Passkey 又剛好在那台裝置上用不了的情境當備援。跟 Passkey 共用同一套「衍生包裝金鑰 → 包住內容金鑰」的邏輯（`FileLocker.Core.Crypto.RecoveryKeyProtector`），差別只在金鑰來源——不是 Windows Hello 簽章，是加密當下產生的一組 256-bit 隨機值，用自訂的 Base32 編碼格式化成人類看得懂、方便抄寫的字串（每 5 字元一組），使用者自己抄下來或存檔保管，FileLocker 本身不留任何副本。

**已排除的替代方案**：
- 安全問題（例如「你的寵物叫什麼名字」）——答案通常查得到或猜得到
- 社交式復原（Shamir's Secret Sharing）——對個人工具過度工程
- 用可跨裝置同步的 Passkey（Microsoft Password Manager）——WebAuthn PRF 擴充功能理論上能做到決定性金鑰衍生，但實測發現同一把同步後的 Passkey 在不同裝置上算出的 PRF 結果不一樣，且 Windows Hello 目前根本沒開放 PRF 這個路徑
- Email 驗證碼——需要伺服器，會把 FileLocker 從「完全在本機、零知識」的架構整個改掉
- 用「同步 Passkey 驗證通過」的訊號去解鎖本機儲存的恢復金鑰——驗證訊號（成功/失敗的布林值）跟本機儲存的祕密之間如果只是靠 if 判斷連接，就等於沒有真正的保護，繞過驗證直接讀本機檔案就能拿到東西，跟 `UserConsentVerifier` + DPAPI 是同一種問題

**安全性**：恢復金鑰本身就是另一把能完全解密內容的鑰匙，安全性等同密碼，GUI 顯示畫面上有明確紅字警告。

**GUI 設計**：加密成功後若有啟用，**強制跳出**恢復金鑰顯示彈窗：紅字警告 + 等寬字體大字顯示恢復碼 + 「複製」「存成檔案」「我已經抄下來了」三個按鈕，「關閉」按鈕預設鎖住，要先做過前三個裡任何一個動作才能關閉。

### 8.3 安全性審查紀錄

**2026-07-23（內部邏輯角度）**：完整檢查過一輪程式碼，重點發現：

- 🔴 **路徑穿越／任意檔案寫入（已修復）**：`RestoreFromKey` 還原檔案原本直接用 `Path.Combine(destinationParentDir, metadata.OriginalName)` 組出目的地路徑。`OriginalName` 來自沒有簽章保護的明文 `.meta.json`，若被竄改成絕對路徑，`Path.Combine` 會忽略第一個參數、直接回傳第二個參數的路徑——等於解密內容可能被寫到任意位置。已加上 `IsSafeRestoreFileName` 檢查（拒絕絕對路徑、拒絕含 `..` 的片段、拒絕含檔名不合法字元）。攻擊門檻是本機已有寫入 `.meta.json` 的能力，不是遠端可觸發，但影響嚴重。
- 🟡 **指標檔清除邏輯加上簽章驗證（已修復）**：解密成功、以及清單頁「永久刪除」後清除原位置失效指標檔的邏輯（`CleanupMarkerIfMatches`），原本只比對 UUID，現在額外要求簽章驗證通過才會執行刪除，避免被偽造的假指標檔誘導誤刪。
- 🟡 **例外處理兜底（已修復）**：加密/解密流程原本只接住 `IOException`／`UnauthorizedAccessException`，其他型別的例外會一路往外傳造成非預期崩潰。加上兜底的 `catch (Exception ex)`；`EncryptAsync` 也補上：metadata 已寫進 Vault、但緊接著寫 marker 失敗時，會盡力清掉這筆孤兒項目。
- 🟢 **極低機率的競態條件（已知，未修）**：加密前檢查「目標位置是否已有指標檔」跟實際寫入之間有間隙，理論上兩個同時對同一路徑的加密請求可能互相覆蓋，需要真的同時觸發才會發生，暫不處理。

**2026-07-24（外部攻擊角度：記憶體安全、輸入攔截、其他攻擊手段）**：

- 🔴🔴 **架構性問題（已修復）：WebView2 曾經載入本機開發伺服器（`http://localhost:5173`），不是封裝好的內容**。原本的風險：任何在本機搶先佔用這個埠的其他程式，都可能讓 `FileLocker.App` 載入一個偽造的假畫面，惡意頁面的 JS 可以直接呼叫 `window.chrome.webview.postMessage(...)` 取得跟真正前端一樣的權限操控後端。**修法**：改用 `#if DEBUG`／`#if !DEBUG` 分支——Debug 建置維持連到 Vite 開發伺服器（方便開發，且只有開發者自己的機器會這樣跑）；Release 建置改用 `SetVirtualHostNameToFolderMapping` 把一個虛擬網址（`https://filelocker.local/`，純粹是本機識別用的標籤，不是真的網域）對應到封裝好的靜態檔案資料夾，整個過程不建立任何網路連線、不透過任何本機埠，`localhost` 埠被搶佔這個攻擊面徹底不存在。`FileLocker.App.csproj` 的 Release 建置流程會自動執行 `npm run build` 並把產出複製進輸出目錄的 `webapp` 資料夾。`NavigationStarting` 的導覽限制、WebView2 密碼自動儲存/自動填入的關閉都保留（Release 也關掉 DevTools，Debug 為了方便開發保留開啟）。
- 🟡 **剪貼簿殘留（已修復）**：恢復金鑰按「複製」之後，Windows 剪貼簿歷史紀錄（`Win+V`）可能會保留這筆等同密碼的內容一段時間，甚至可能跨裝置同步。比照密碼管理工具的慣例，複製後 45 秒自動清空剪貼簿（只有剪貼簿內容還是剛剛複製的那份時才清，避免蓋掉使用者後來自己複製的東西）。
- 🟡 **部分密碼輸入用明碼顯示的 `prompt()`（已修復）**：已加密清單頁「解密」「全部解鎖」原本用瀏覽器原生 `prompt()` 輸入密碼，打字內容明碼顯示在螢幕上（不像 `<input type="password">` 會用點點遮起來），旁邊有人看、或畫面被錄影/遠端連線時直接看得到。已全部改用統一的遮罩密碼輸入彈窗，跟加密/解密頁籤表單一致。
- 🟡 **C++ 命令列組裝的引號逃脫問題（已修復）**：Shell Extension 組命令列時原本單純把每個路徑前後包雙引號，沒有處理「路徑結尾是奇數個反斜線、緊接著右引號」這個 Windows 命令列解析的已知邊界情況（那個反斜線會逃脫掉補上去的關閉引號，導致參數沒有真的結束、後續參數解析錯亂）。已實作正確的引號逃脫演算法（`QuoteArgument`，比照微軟官方文件的標準做法）。
- 🟡 **暫存批次清單檔用完沒清除（已修復）**：Shell Extension 批次項目超過門檻時寫進 `%TEMP%` 的暫存清單檔，`FileLocker.App` 讀取後原本沒有刪除，內容是使用者選了哪些檔案路徑（不是內容，但路徑本身也可能透露敏感資訊）。已在讀取完成後刪除。
- 🟡 **Argon2 密碼位元組陣列沒有主動清零（已修復）**：`Encoding.UTF8.GetBytes(password)` 轉出來、直接丟進 Argon2id 建構子用的中間位元組陣列，原本沒有明確清零，用完會比必要的時間活得更久。已改成 `try/finally` 包住、用完主動 `CryptographicOperations.ZeroMemory`。
- 🟢 **鍵盤側錄（已知限制，非本工具能解決）**：如果作業系統本身已經被鍵盤側錄軟體/惡意程式滲透，任何本機應用程式的密碼輸入都無法防禦這類攻擊，這是所有本機密碼系統共同的極限，不是 FileLocker 特有的弱點，也沒有應用程式層級的解法。
- 🟢 **C++ 記憶體安全逐項檢查，未發現額外問題**：`DragQueryFileW` 用固定緩衝區但呼叫方式正確不會溢位（過長路徑會被截斷、不是溢位，屬於功能限制不是安全漏洞）；COM 物件的參照計數（`AddRef`/`Release`）邏輯正確；`GetTempFileNameW` 建立暫存檔本身有防止檔名競爭的機制；標準系統函式庫（`ole32`／`advapi32`／`shell32`／`user32`）都受 Windows KnownDLLs 機制保護，不受 DLL 搜尋順序挾持影響。

**2026-07-24（第三輪：單一執行個體機制、雲端同步修復後的複查）**：

- 🟡 **單一執行個體的 Named Pipe 沒有存取限制（已修復）**：原本用預設設定建立的 Named Pipe，理論上同一台機器上其他登入的使用者（快速切換使用者、遠端桌面）都能連進來，塞任意路徑給正在跑的 FileLocker 實體（頂多是讓加密頁籤被動帶入不明路徑，使用者還是要自己按加密才會真的執行，但仍是不必要的跨使用者互動）。已加上 `PipeOptions.CurrentUserOnly`（Server 端與 Client 端都要加），限制成只有目前這個 Windows 使用者能連進來，這是 .NET 官方提供的內建機制，不需要自己手動組存取控制清單。
- 🟡 **`VaultManager.ScanAll()` 遇到真正的雲端同步分歧時判斷依據不明確（已修復）**：先前修掉的「衝突副本重複顯示」問題，解法是同一個 UUID 只回傳第一個列舉到的，但列舉順序不保證固定；如果兩份檔案是「真正的分歧」（不是單純重複副本，而是兩台裝置各自做了不同修改），回傳哪一份其實有差別，原本的做法等於隨機。已改成比較檔案的最後寫入時間，同一個 UUID 只回傳寫入時間最新的那份，行為變成決定性、可預期（偏好保留較新的變更）。
- 🟢 **Release 模式找不到打包好的前端靜態檔案時，畫面會直接空白（已修復，屬於健壯性強化而非安全漏洞）**：加上明確的錯誤訊息，指出預期的資料夾位置，方便排查是不是忘記跑建置流程。

### 8.4 密碼錯誤鎖定機制（已完成）

只套用在**密碼**這條路徑——Passkey 每次都要真的通過 Windows Hello，恢復金鑰是 256-bit 高熵值（暴力破解在數學上不可行），都不需要額外鎖定。

實作在 `FileLocker.Core.Security.LockoutTracker`，狀態存在 `%LocalAppData%\FileLocker\lockout.json`（不在 Vault 裡，不隨雲端同步，這是「這台裝置」的安全狀態）：
- 每個項目（依 UUID）各自獨立累計失敗次數，成功解鎖一次會清掉該項目的失敗紀錄
- 累計失敗達 **5 次**開始鎖定，鎖定時間隨累積失敗次數遞增（30 秒 → 60 秒 → 120 秒...，上限 1 小時），拖慢持續嘗試的攻擊者
- 鎖定期間就算輸入正確密碼也會被擋下來，直到鎖定時間過去

---

## 9. GUI 設計

前端技術：HTML + CSS + WebView2 host + **Vue 3（Composition API）**，用 Vite 建置，透過 WebView2 的 JS-C# Bridge（`postMessage`）跟後端溝通。**目前是功能優先的陽春樣式（行內 style），視覺美化刻意留到功能都穩定後一次處理**（見第 12 節）。

主視窗四個頁籤：

- **加密**：可一次選多個檔案/資料夾（多選檔案對話框 + 可重複點「選擇資料夾」加入），列出待加密清單、可個別移除。選超過一個項目時 Passkey／恢復金鑰勾選框自動鎖住（見 3.5 節）。送出後即時顯示每個項目的加密進度與結果。
- **解密**：手動選 `.locked` 檔案 + 輸入密碼；選檔後會自動查詢該項目是否也支援 Passkey／恢復金鑰，支援的話額外顯示對應的快速解鎖按鈕，還原位置固定用 `.locked` 檔案目前所在的資料夾。
- **已加密清單**：兩個子頁籤：
  - **已加密檔案**：讀取 Vault 目前實際存在的項目。每列顯示名稱、型別、大小、提示、加密時間，可用密碼／Passkey／恢復金鑰個別解鎖（解鎖前可選擇還原到原始位置或自訂位置），或「永久刪除」（明確告知這是不可逆銷毀 Vault 內容，非移除清單顯示）。同一批次加密的項目摺疊成一組顯示，可展開個別操作或「全部解鎖」。指標檔如果在預期位置找不到，會顯示警示文字。
  - **使用紀錄**：本機操作日誌（`history.jsonl`），跟 Vault 目前狀態無關，項目就算已經解密或刪除，紀錄仍然保留。顯示名稱、動作（加密/解密/刪除紀錄）、時間，以及依動作類型顯示的細節（加密：來源路徑、Passkey/恢復金鑰是否啟用；解密：用哪種方式解的鎖、還原到哪裡）。
- **設定**：Vault 目前位置顯示 + 搬移按鈕（見第 6 節）；語言下拉（目前只有繁體中文一個選項）；主題按鈕（亮色/深色，目前只存偏好，畫面還沒真的套用）。

**密碼輸入小視窗**（`PasswordPromptWindow`，原生 WPF，不透過 WebView2）：雙擊 `.locked` 檔案時跳出。如果該項目有啟用 Passkey，視窗一開啟就**自動**觸發 Windows Hello 驗證；使用者把驗證視窗關掉（放棄這次嘗試）才會退回密碼輸入畫面，同時保留「使用 Passkey 解鎖」按鈕讓使用者可以重試。

視覺語言參考 Apple HIG（留白充足、清楚層級、克制動效）跟 `emilkowalski/skills` 的互動細節做法——**這部分等 GUI 美化階段才會真的套用**，目前的畫面不反映最終呈現。

---

## 10. 多語言（已完成前端靜態文字，後端訊息仍是已知限制）

支援**繁體中文（`zh-TW`）／英文（`en`）**兩個語言。實作方式：`src/FileLocker.Web/src/locales/` 底下 `zh-TW.json`、`en.json` 兩份語言包（key-value 對應），`App.vue` 內建一個簡單的 `t(key, params)` 翻譯函式（找不到對應語言檔或找不到 key 時退回繁體中文，`{name}` 這種花括號佔位符用來塞動態內容），前端全部靜態文字（頁籤名稱、按鈕、標籤、表格欄位、彈窗、確認/警告訊息）都改用 `t()` 呼叫。語言選擇存在 `AppSettings.Language`（沿用既有欄位），設定頁的語言下拉選單即時切換；**App 啟動時就會主動要一次設定值套用語言，不是等使用者自己點進設定頁才生效**，確保重開 App 後直接顯示上次選擇的語言。

**已知限制**：**後端（C#）回傳的錯誤訊息目前仍然固定是繁體中文**（例如密碼錯誤、鎖定中的提示文字，這些直接來自 `LockService` 內部產生的字串）。原本規劃的做法是後端只傳統一的 error code、前端負責轉譯顯示文字，但這代表要把後端目前上百處直接組字串回傳的錯誤訊息全部重構成代碼，工程量遠超单純加語言包這件事，這次沒有一併處理，是明確記錄下來的已知限制，之後若要支援英文使用者看懂完整的錯誤訊息，需要另外排時間做這個後端重構。

語言選單裡每個語言的選項名稱，刻意固定用該語言自己的名稱顯示（例如「繁體中文」不會因為目前介面是英文就被翻譯成「Traditional Chinese」），這是語言選擇器的標準慣例做法。

---

## 11. 版本相容性策略

- **後端執行環境**：.NET 10，使用 `self-contained` 發布模式打包執行環境進安裝檔，不需要額外安裝 .NET Runtime。
- **前端執行環境**：WebView2 Runtime，現代 Windows 幾乎都內建，缺少時用官方 Bootstrapper 自動補裝。
- **最低系統需求**：Windows 10 1809 以上（WebView2 支援下限），不特別支援 Windows 7/8。
- **COM Shell Extension**：需要區分 32/64 位元 DLL 註冊（Windows Shell Extension 機制本身的硬性要求），目前開發環境確認編譯/註冊 64 位元版本可行，安裝程式階段要處理依系統架構選擇對應 DLL。

---

## 12. 開發階段規劃與進度

| 階段 | 內容 | 狀態 |
|---|---|---|
| 1 | Core Engine：檔案/資料夾加密解密邏輯、UUID 機制、Argon2+AES-GCM 串流加密、Zip 封裝、單元測試 | ✅ 已完成 |
| 2 | Metadata 層：`.meta.json` 讀寫 | ✅ 已完成；原規劃的 SQLite 快取層／`FileSystemWatcher` 評估後不需要，用清單頁手動「重新整理」取代 |
| 3 | CLI 原型 | ✅ 已完成（`FileLocker.Cli`） |
| 4 | WebView2 + Vue 3 前端：加密（含批次）／解密／已加密清單／使用紀錄／設定／密碼小視窗 | ✅ 已完成，含三條解鎖路徑（密碼/Passkey/恢復金鑰）與批次群組摺疊 |
| 5 | Shell Extension（C++）：右鍵選單、多選批次支援、自動註冊 | ✅ 已完成並實測：COM 註冊（`HKEY_CURRENT_USER`，不需要系統管理員權限）、`IShellExtInit` 多選清單擷取、`IContextMenu` 選單顯示與點擊、超過門檻切換暫存清單檔、真的呼叫 `FileLocker.App.exe` 帶入路徑、App 啟動時自動檢查並完成登錄，都驗證過。安裝程式階段只需要把 DLL 跟 exe 放進同一個資料夾，不需要額外邏輯 |
| 6 | `.locked` 副檔名關聯（正式安裝腳本） | 待開始（開發階段用手動命令列參數測試雙擊行為） |
| 7 | 安全性強化：安全清除、鎖定機制、簽章驗證、Passkey、恢復金鑰、資料安全性審查（含外部攻擊角度） | ✅ 已完成 |
| 8 | 雲端同步情境測試（模擬多裝置同步、衝突情境） | 🟡 FileLocker 自己能控制的部分已完成並測試：`VaultManager.ScanAll()` 修正衝突副本重複顯示的問題、Vault 可攜性驗證（整個資料夾搬到新位置仍可正常解密）、多實體同時操作同一項目不會崩潰或損毀資料。真的跨裝置/雲端帳號的實測待使用者自行驗證 |
| 9 | 多語言、打包安裝程式（含 32/64 位元 Shell Extension DLL、登錄腳本） | 🟡 多語言前端靜態文字已完成（繁體中文／英文），後端動態錯誤訊息仍是已知限制（見第 10 節）；打包安裝程式待開始 |
| — | GUI 視覺美化（套用 Apple HIG／`emilkowalski/skills` 動畫規則） | 待開始，刻意留到功能都穩定後一次處理 |

---

## 13. 安裝程式打包（後期階段）

最終編譯完成的 `FileLocker.App.exe`（含 C# 主程式）+ `FileLockerShellExtension.dll`（C++ Shell Extension）打包成安裝檔的部分，直接沿用 **[mac-style-windows-installer](https://github.com/Lai-xuan/mac-style-windows-installer)**（Python + PyInstaller + pywebview 寫的 macOS 風格拖曳安裝視窗產生器）。已經對照過這個工具的使用說明書，確認幾個跟 FileLocker 對接相關的重點：

- **檔案關聯已內建支援**：製作安裝檔時勾選「檔案關聯」欄位、填副檔名，安裝時就會自動寫入 `HKCR`——`.locked` 副檔名關聯（第 5.3 節）可以直接透過這個欄位設定，不需要自己另外寫登錄腳本。實際對接時要確認一下這個工具產生出來的開啟指令格式是不是 `"...\FileLocker.App.exe" "%1"`（把檔案路徑當參數傳進去），這點文件沒有寫死說明，需要實際排到這個階段時操作一次確認。
- **Shell Extension DLL 的安裝已經不需要這個工具做任何特殊處理**：原本擔心這個工具不支援 COM／`regsvr32` 這類註冊機制，現在已經解決——`FileLocker.App` 會自己在啟動時檢查並完成登錄（見第 5.2 節「自動註冊」），安裝程式只要把 `FileLockerShellExtension.dll` 當成普通檔案，跟 `FileLocker.App.exe` 一起放進「應用程式內容資料夾」就好，不需要額外的安裝邏輯。
- **不會誤刪 Vault**：解除安裝只刪 `install_manifest.json` 記錄的檔案（也就是安裝目錄底下的東西），FileLocker 的 Vault 預設在 `%LocalAppData%\FileLocker\Vault`，不在安裝目錄裡，解除安裝不會動到使用者的加密內容——這點在文件裡確認過，是好消息，不用另外處理。
- **沒有數位簽章**：使用者第一次執行安裝檔，Windows SmartScreen 大機率會跳警告。要解決需要另外採購程式碼簽署憑證，這不是安裝程式工具本身能解決的事，先讓使用者知道會有這個警告、確認來源後繼續即可。這也代表**目前完全沒有偵測執行檔本身是否被竄改的機制**——數位簽章除了消除 SmartScreen 警告，更重要的作用是讓 Windows 能自動驗證執行檔完整性（簽章綁定檔案內容，任何位元組被改動簽章就會失效），這是業界標準、真正有效的保護方式，但需要外部採購憑證的商業流程，不是程式碼能解決的。曾考慮過「程式自己在啟動時檢查自身雜湊值」這種做法，但評估後認為防護力太弱——攻擊者只要能竄改執行檔內容，同樣能連檢查邏輯本身一起改掉，只能擋住意外損毀、擋不住真正有心的竄改，容易給人錯誤的安全感，決定不做，留待日後採購數位簽章憑證時一併處理。
- **相依元件（WebView2 Runtime）只做偵測 + 提示下載連結，不會自動安裝**，跟第 11 節提過的「WebView2 缺少時用官方 Bootstrapper 補裝」是兩件事——這個安裝程式工具本身不會幫忙補裝，只會提醒使用者，需要在文件裡跟使用者說清楚。
- 支援靜默安裝命令列參數（`/S`、`/D=路徑`、`/NODESKTOPSHORTCUT`），對個人工具來說不是必要功能，但如果之後有企業批次部署的需求，這條路已經現成可用。

---

## 14. 仍待確認事項

- 安裝程式需要在安裝流程中內建重啟 Explorer 的提示步驟，細節待該階段再對接確認
- 解密頁面是否要加多選（一次選多個各自獨立加密過的 `.locked` 檔案，用同一組密碼解密）——已經討論過，決定**不加**，因為跟已加密清單頁的批次群組解鎖功能重疊度高，暫不處理。