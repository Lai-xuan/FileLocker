<Window x:Class="FileLocker.App.PasswordPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FileLocker" Height="230" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="True">
    <StackPanel Margin="20">
        <TextBlock x:Name="FileNameText" FontWeight="Bold" FontSize="14" Margin="0,0,0,4" TextWrapping="Wrap" />
        <TextBlock x:Name="HintText" Foreground="Gray" Margin="0,0,0,12" TextWrapping="Wrap" />
        <PasswordBox x:Name="PasswordInput" Padding="6" FontSize="13" KeyDown="PasswordInput_KeyDown" />
        <TextBlock x:Name="ErrorText" Foreground="Red" Margin="0,8,0,0" TextWrapping="Wrap" Visibility="Collapsed" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="CancelButton" Content="取消" Width="80" Margin="0,0,8,0" Click="CancelButton_Click" />
            <Button x:Name="UnlockButton" Content="解密" Width="80" Click="UnlockButton_Click" IsDefault="True" />
        </StackPanel>
    </StackPanel>
</Window>
