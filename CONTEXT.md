# FileLocker

Windows 檔案／資料夾保護工具。提供兩種互相獨立、安全等級不同的保護機制：**加密**（真正的密碼學保護，內容經 AES-256-GCM 加密後移入 Vault）與**資料夾防護**（純 Windows ACL 存取限制，內容不加密、原地保留）。

## Language

**加密（Encrypt）**：
把檔案／資料夾內容用 Argon2id 衍生金鑰 + AES-256-GCM 加密，移至 Vault 集中管理，原位置留下 `.locked` 指標檔。這是系統既有、最強的保護等級——內容離開持有者電腦（例如硬碟被拆走）依然無法被讀取。
_Avoid_: 鎖定、上鎖（這兩個詞在 UI 上保留給下面的「資料夾防護」使用，加密功能對使用者只稱「加密／解密」，不稱「鎖定」）

**Vault**：
加密內容集中存放的管理區，只有加密功能會寫入。資料夾防護不使用 Vault。

**`.locked` 指標檔**：
加密後留在原位置的指標檔，內容經 HMAC-SHA256 簽章。這個副檔名跟機制專屬於加密功能，資料夾防護不產生 `.locked` 檔案（防護中的資料夾原地保留，不會被搬走或替換成指標檔）。

**資料夾防護（Folder Guard）**：
透過拒絕目前 Windows 帳號在該資料夾上的 ACL 權限來限制存取，不加密內容、資料夾原地保留不搬動、不需要提權（見 [ADR-0001](docs/adr/0001-folder-guard-deny-acl-not-ownership-transfer.md)）。是比「加密」弱、刻意接受的保護等級——防的是「不小心/隨手打開」，不是「蓄意繞過權限」或「離線讀取硬碟」；擁有者本人隨時能自行改回權限，不需要密碼。獨立分頁，跟「加密」分頁區隔，統一管理所有上鎖中的資料夾。
_Avoid_: 鎖定資料夾（跟既有「鎖定＝加密」語彙衝突）、限制存取（备选但未採用）

**資料夾防護密碼／Passkey**：
整個「資料夾防護」功能共用同一組（不是每個資料夾各自一組，跟「加密」分頁的每項目獨立密碼不同）。密碼必填（第一次上鎖任何資料夾前強制設定），Passkey 選配、密碼永遠是保底解鎖手段。清單頁可個別解鎖單一資料夾，或用獨立的「解鎖全部」按鈕一次解鎖所有項目（需二次確認）。重設密碼／Passkey 直接套用到所有正在上鎖中的資料夾，不需要先解鎖——因為這組憑證只是身份驗證雜湊，不是解鎖資料夾內容用的鑰匙（ACL 拒絕規則本身不是從密碼算出來的）。即使密碼與 Passkey 都遺失，使用者仍能透過 Windows 內建的檔案總管「安全性」設定拿回資料夾存取權，這點會在設定頁主動告知，跟「加密」的「密碼遺失＝無法復原」是刻意做出的差異。

**上鎖／解鎖（Lock / Unlock）**：
「資料夾防護」分頁內的動作動詞，指設定／解除 ACL 限制。這兩個字只在資料夾防護分頁使用；加密功能的對應動作固定稱「加密／解密」，兩邊動詞完全不共用，避免使用者混淆兩種保護等級。