<Window x:Class="FileLocker.App.FolderGuardUnlockPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FileLocker" Width="420" SizeToContent="Height" MinHeight="200"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        FontFamily="./Assets/Fonts/#IBM Plex Sans TC, Segoe UI, Microsoft JhengHei UI, Microsoft JhengHei"
        Background="{DynamicResource SurfaceBrush}"
        SourceInitialized="Window_SourceInitialized"
        PreviewKeyDown="Window_PreviewKeyDown">

    <!--
        右鍵「解鎖」的原生小視窗：技術結構、資源字典比照 FolderGuardConfirmLockWindow／
        PasswordPromptWindow（無邊框、DwmSetWindowAttribute 圓角、DragMove 拖曳、Esc 關閉）。
        跟上鎖的瞬間確認彈窗不同，解鎖一定要驗證身份——Passkey 已啟用時只顯示 Passkey 相關 UI，
        不顯示密碼欄位（資料夾防護所有驗證操作都是 Passkey-only，沒有自動退回密碼，只有設定頁
        的「停用 Passkey」是例外escape hatch），沒有 Passkey 時才顯示密碼輸入。
    -->

    <Window.Resources>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="WindowBorderBrush" Color="#E1E4EA" />
        <SolidColorBrush x:Key="BorderStrongBrush" Color="#C9CDD6" />
        <SolidColorBrush x:Key="TextBrush" Color="#1B1E24" />
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#454A54" />
        <SolidColorBrush x:Key="TextTertiaryBrush" Color="#6B707A" />
        <SolidColorBrush x:Key="AccentBrush" Color="#A8770F" />
        <SolidColorBrush x:Key="DangerBrush" Color="#B14328" />

        <Style x:Key="ButtonBase" TargetType="Button">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Padding" Value="26,14" />
            <Setter Property="MinWidth" Value="128" />
            <Setter Property="MinHeight" Value="46" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                RenderTransformOrigin="0.5,0.5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.88" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.75" />
                                <Setter TargetName="Bg" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.97" ScaleY="0.97" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonBase}">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonBase}">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderStrongBrush}" />
        </Style>

        <Style x:Key="PasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Padding" Value="12,10" />
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="CaretBrush" Value="{DynamicResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderStrongBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TrafficLightCloseStyle" TargetType="Button">
            <Setter Property="Width" Value="12" />
            <Setter Property="Height" Value="12" />
            <Setter Property="Background" Value="#FF5F57" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse x:Name="Circle" Fill="{TemplateBinding Background}" />
                            <TextBlock x:Name="Glyph" Text="&#x2715;" FontSize="8" FontWeight="Bold"
                                       Foreground="#7A1300" Opacity="0"
                                       HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Glyph" Property="Opacity" Value="1" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Circle" Property="Opacity" Value="0.7" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border BorderBrush="{DynamicResource WindowBorderBrush}" BorderThickness="1">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid x:Name="TitleBar" Grid.Row="0" Height="20" Margin="0,0,0,14"
                  Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Button x:Name="CloseButton" Style="{StaticResource TrafficLightCloseStyle}"
                        HorizontalAlignment="Left" VerticalAlignment="Center" Click="CancelButton_Click" />
            </Grid>

            <TextBlock Grid.Row="1" Text="解鎖資料夾" FontSize="18" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextBrush}" Margin="0,0,0,10" />

            <TextBlock x:Name="MessageText" Grid.Row="2" TextWrapping="Wrap" FontSize="13.5"
                       Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,16" />

            <!-- Passkey 模式：只有狀態文字，沒有密碼欄位（Passkey-only，沒有自動退回密碼）。 -->
            <TextBlock x:Name="PasskeyStatusText" Grid.Row="3" TextWrapping="Wrap" FontSize="13"
                       Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,16"
                       Visibility="Collapsed" />

            <!-- 密碼模式：Passkey 未啟用時才顯示。 -->
            <StackPanel x:Name="PasswordPanel" Grid.Row="3" Margin="0,0,0,16" Visibility="Collapsed">
                <TextBlock Text="密碼" FontSize="12.5" Foreground="{DynamicResource TextTertiaryBrush}" Margin="0,0,0,6" />
                <PasswordBox x:Name="PasswordInput" Style="{StaticResource PasswordBoxStyle}"
                             PasswordChanged="PasswordInput_PasswordChanged" />
            </StackPanel>

            <TextBlock x:Name="ErrorText" Grid.Row="4" TextWrapping="Wrap" FontSize="12.5"
                       Foreground="{DynamicResource DangerBrush}" Margin="0,0,0,14" Visibility="Collapsed" />

            <Grid Grid.Row="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" x:Name="CancelButton" Content="取消" Style="{StaticResource SecondaryButtonStyle}" Click="CancelButton_Click" />
                <Button Grid.Column="2" x:Name="UnlockButton" Content="解鎖" Style="{StaticResource PrimaryButtonStyle}"
                        IsEnabled="False" Click="UnlockButton_Click" />
            </Grid>

            <!-- 成功收尾動畫，疊在整個 Grid 上面，跟 PasswordPromptWindow 的 SuccessOverlay 同一招。 -->
            <Border x:Name="SuccessOverlay" Grid.Row="0" Grid.RowSpan="6"
                    Background="{DynamicResource SurfaceBrush}" Opacity="0" Visibility="Collapsed">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="&#x2713;" FontSize="36" FontWeight="Bold"
                               Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center" />
                    <TextBlock Text="已解鎖" FontSize="14" Margin="0,8,0,0"
                               Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Center" />
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
